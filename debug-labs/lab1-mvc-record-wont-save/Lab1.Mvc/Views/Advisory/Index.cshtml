@model IEnumerable<Lab1.Mvc.Models.AdvisoryModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdvisoryID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdvisoryText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdvisoryID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdvisoryText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.AdvisoryID }) |
            @Html.ActionLink("Details", "Details", new { id = item.AdvisoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.AdvisoryID })
        </td>
    </tr>
}

</table>
